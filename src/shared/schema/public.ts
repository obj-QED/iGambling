export { filterKeysByCapabilities, isCapabilityEnabled } from './capabilities';
export { deepMerge, mergeSchemaLayers } from './mergeLayers';
export { resolveWidgetSchema } from './resolveSchema';
export type {
  BaseWidgetSchema,
  BehaviorFlags,
  ResolveSchemaOptions,
  SchemaLayers,
  SchemaVersion,
  WrapperMode,
} from './types';
export { SCHEMA_VERSIONS, WRAPPER_MODES } from './types';
