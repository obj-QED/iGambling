/**
 * Thin alias over Mantine {@link https://mantine.dev/core/menu/ Menu} (`AppMenu.*` mirrors `Menu.*`).
 * Behaviour and styling match Mantine defaults unless you extend `Menu` in the theme or pass props.
 */
export type { AppMenuProps } from './types/AppMenu.types';
export { AppMenu } from './ui/AppMenu';
