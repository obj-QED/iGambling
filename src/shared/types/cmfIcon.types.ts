/** Square (1:1) or rectangular media — API may send other values as-is. */
export type CmfIconShape = 'square' | 'rect';

/** `round` = 100%; `sm` = theme token (6px). */
export type CmfIconRadius = 'round' | 'sm';
