/**
 * @deprecated Use {@link controlAttrs} from `@/shared/lib` (or `@/shared/lib/cmf`).
 * Re-export kept so gradual migrations / external imports do not break.
 */
export {
  controlAttrs as menuControlAttrs,
  type ControlAttrsOptions as MenuControlAttrsOptions,
  type ControlAttrsItem as MenuControlItem,
} from '@/shared/lib/cmf';
