# Grouped PRs — one per ecosystem so CI is not flooded with N× workflows.
# Lives on the default branch (`main`). Auto-merge waits for required CI / reviews.
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 1
    groups:
      all-npm:
        patterns:
          - '*'
    labels:
      - dependencies
      - security
    assignees:
      - obj-QED
    commit-message:
      prefix: 'deps'
      include: 'scope'

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 1
    groups:
      all-actions:
        patterns:
          - '*'
    labels:
      - dependencies
      - github-actions
    assignees:
      - obj-QED
    commit-message:
      prefix: 'ci'
      include: 'scope'
